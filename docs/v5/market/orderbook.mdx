---
title: Get Orderbook
sidebar_label: Get Orderbook
sidebar_position: 5
---

Query orderbook data
> **Covers: Spot / Linear contract / Inverse contract / Option**

* future: A maximum of 200-level of orderbook data can be returned.
* spot: A maximum of 50-level of orderbook data can be returned.
* option: 25-level of orderbook data can be returned ONLY.

:::tip
The response is in the snapshot format.
:::

### HTTP Request
GET `/v5/market/orderbook`

### Request Parameters
| Parameter | Required | Type | Comments|
|:----- |:-------|:-----|------ |
|[category](../enum#category) |**true** |string |Product type. `spot`,`linear`,`inverse`,`option` |
|symbol |**true** |string |Symbol name|
|limit |false |integer |Limit for data size per page.<ul><li>`spot`: [1,50], default: `1`.</li><li>`linear`&`inverse`: [1,200],default: `1`.</li><li>`option`: [1,25],default: `1`.</li></ul>|

### Response Parameters
| Parameter | Type | Comments|
|:----- |:-----|----- |
|s |string |Symbol name |
|b |array |Bid, buyer. Sort by price desc |
|> b[0] |string |Bid price |
|> b[1] |string |Bid size |
|a |array |Ask, seller. Order by price asc |
|> a[0] |string |Ask price |
|> a[1] |string |Ask size |
|ts |integer |The timestamp (ms) that system generates the data |
|u |integer |Update ID, is always in sequence |

### Request Example

```http
GET /v5/market/orderbook?category=spot&symbol=BTCUSDT HTTP/1.1
Host: api-testnet.bybit.com
```

### Response Example

```json
{
    "retCode": 0,
    "retMsg": "OK",
    "result": {
        "s": "BTCUSDT",
        "a": [
            [
                "16638.64",
                "0.008479"
            ]
        ],
        "b": [
            [
                "16638.27",
                "0.305749"
            ]
        ],
        "ts": 1672765737733,
        "u": 5277055
    },
    "retExtInfo": {},
    "time": 1672765737734
}
```