---
title: Create Subaccount Transfer
sidebar_label: Create Subaccount Transfer
---
import TOCInline from '@theme/TOCInline';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

> Request Example

<Tabs>

<TabItem value="console" label="Console">

```console
curl POST 'https://api-testnet.bybit.com/asset/v3/private/transfer/sub-member-transfer' \
--header 'Content-Type: application/json' \
--header 'X-BAPI-TIMESTAMP: 1660794864052' \
--header 'X-BAPI-API-KEY: XXXXXXXXXXXXXX' \
--header 'X-BAPI-RECV-WINDOW: 999999999' \
--header 'X-BAPI-SIGN: c4f6cbc0581a8afa81cade431c9389a62ff7817e3e6c4aee456fd3f86796f619' \
--data-raw '{
    "transferId": "xxxxxxx",
    "coin": "BTC",
    "amount": "0.11",
    "subMemberId": 12,
    "type": "IN",
}'
```

</TabItem>

<TabItem value="py" label="Python">

```python live
import requests
url = "https://api-testnet.bybit.com/derivatives/v3/public/order-book/L2?category=linear&symbol=BTCUSDT"
response = requests.request("GET", url)
return response.text
```

</TabItem>

<TabItem value="js" label="JavaScript">

```jsx live
function Clock(props) {
  const [date, setDate] = useState(new Date());
  useEffect(() => {
    const timerID = setInterval(() => tick(), 1000);

    return function cleanup() {
      clearInterval(timerID);
    };
  });

  function tick() {
    setDate(new Date());
  }

  return (
    <div>
      <h2>It is {date.toLocaleTimeString()}.</h2>
    </div>
  );
}
```

</TabItem>
</Tabs>

> Response Example

<Tabs>

<TabItem value="js linear" label="JavaScript linear">

```javascript
{
  "retCode":0,
    "retMsg":"OK",
    "result":{
    "transferId": "xxxxxxxxxxx"
  },
  "retExtInfo":{},
  "time":1652841868446
}
```

</TabItem>

<TabItem value="js option" label="JavaScript option">

```javascript
{
  "retCode":0,
    "retMsg":"OK",
    "result":{
    "transferId": "xxxxxxxxxxx"
  },
  "retExtInfo":{},
  "time":1652841868446
}
```

</TabItem>

</Tabs>

Transfers funds between the parent and child (sub) accounts.
To send a transfer which is account type & parent/sub agnostic, use the Create Universal Transfer endpoint.

:::info
A subaccount can only be attributed to a parent account.
:::

#### HTTP Request
POST <code>/asset/v3/private/transfer/sub-member-transfer</code>

#### Request Parameters
| Parameter | Required | Type | Comments|
|:----- |:-------|:-----|------ |
|transferId |<b>true</b> |string |UUID, which is unique across the platform |
|coin |<b>true</b> |string |Currency type |
|amount |<b>true</b> |string |Exchange to amount |
|subMemberId |<b>true</b> |string |Subaccount |
|type |<b>true</b> |string |Determines the direction of transfer |

#### Response Parameters
| Parameter | Type | Comments|
|:----- |:-----|----- |
|transferId |string |UUID, which is unique across the platform |